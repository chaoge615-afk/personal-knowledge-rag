# 第一阶段：个人知识库RAG项目 - 开发规则与学习检查清单

## 📋 学习目标

完成本阶段后，你将掌握：
- ✅ RAG（检索增强生成）完整工作流程
- ✅ LangChain 核心组件用法（文档加载器、分块器、向量库、检索链）
- ✅ Embedding 原理和实践
- ✅ 通义千问API调用方式
- ✅ 如何调优RAG效果（分块大小、检索数量、prompt工程）

---

## ✅ 已完成

| 项 | 状态 |
|---|------|
| 项目框架搭建 | ✅ 完成 |
| 环境变量配置 | ✅ 完成 |
| RAG引擎核心类 `KnowledgeRAG` | ✅ 完成 |
| 交互式命令行入口 `main.py` | ✅ 完成 |
| `.gitignore` 配置 | ✅ 完成 |
| requirements.txt 依赖列表 | ✅ 完成 |

---

## ⚙️ 待开发任务（按优先级）

### P0 - 必须完成（让项目跑通可用）
- [ ] 填入通义千问API Key到 `.env`
- [ ] 创建虚拟环境并安装依赖 `pip install -r requirements.txt`
- [ ] 放入至少5个你的markdown笔记到 `knowledge/` 目录
- [ ] 运行 `python main.py` → `load` → 测试提问，确认正常工作

### P1 - 基础优化
- [ ] 根据实际效果调整 `CHUNK_SIZE`（推荐范围：300-1000）
- [ ] 调整检索数量 `search_kwargs={"k": 5}`（推荐3-8）
- [ ] 优化prompt，让回答更符合你的使用习惯
- [ ] 添加 `.env.example` 模板文件，方便别人部署

### P2 - 功能扩展（选做，提升项目亮点）
- [ ] **自动扫描**：监控 `knowledge/` 目录变化，文件修改自动更新索引
- [ ] **Web界面**：添加一个简单的Streamlit/Gradio Web界面，比命令行好用
- [ ] **导出回答**：支持将问答对话导出为markdown文件保存
- [ ] **元数据过滤**：给不同文件加上标签，支持按标签筛选检索
- [ ] **多路召回**：尝试不同分块策略，融合多个检索结果

---

## 📐 开发规则

### 代码风格
- 遵循 [PEP 8](https://peps.python.org/pep-0008/)
- 使用4空格缩进，不使用tab
- 每个函数必须有docstring说明功能
- 复杂逻辑必须加注释

### 配置管理
- **禁止**：API Key提交到Git
- **必须**：所有配置通过环境变量，从 `.env` 读取
- **必须**：提供 `.env.example`，去掉敏感信息

### 提交规范
- 每次完成一个小功能就提交
- 提交信息格式：`feat: 添加xxx功能` / `fix: 修复xxx问题`
- 示例：`feat: 添加自动扫描knowledge目录功能`

---

## 🔍 RAG调优指南

### 分块大小调优
| 分块大小 | 适用场景 | 优点 | 缺点 |
|---------|---------|------|------|
| 200-500 | 短文档、问答类 | 粒度细，检索准 | 上下文不够 |
| 500-1000 | 一般技术文档 | 平衡 | 推荐默认 |
| 1000-2000 | 长文、教程 | 上下文完整 | 检索不够精准 |

**调优方法**：从 500 开始试，看回答是不是太碎片化，再慢慢调大。

### 检索数量k调优
- 太少（k<3）：信息不全，可能找不到答案
- 太多（k>10）：引入噪音，LLM抓不住重点
- **推荐**：k = 3~8

### Prompt调优要点
- 明确告诉AI："如果找不到答案就直说，不要编造"
- 如果需要特定格式回答，在prompt里指定格式
- 中文项目，prompt用中文写，效果更好

当前prompt已经调好，可以先试用。

---

## ✅ 验收标准

第一阶段完成的标志：

1. **功能可用**
   - [ ] 能正常加载你的markdown笔记
   - [ ] 提问能给出准确回答
   - [ ] 找不到答案时会明确说找不到

2. **项目完整**
   - [ ] `README.md` 有完整使用说明
   - [ ] `requirements.txt` 依赖完整
   - [ ] `.env.example` 配置模板存在
   - [ ] 没有敏感信息提交到Git

3. **效果满意**
   - [ ] 回答准确率 > 80%（提问10个问题，至少8个回答正确）
   - [ ] 回答速度能接受（一般几秒钟）

当你满足以上所有条件，第一阶段就算完成，可以进入第二阶段Text-to-SQL了。

---

## 🚀 快速开始（给你自己看，也会写进README）

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置API Key
cp .env.example .env
# 编辑 .env，填入你的 DASHSCOPE_API_KEY

# 4. 放入你的markdown笔记
# 把markdown文件放到 knowledge/ 目录

# 5. 运行
python main.py

# 首次运行输入 load 加载知识库
# 然后直接提问就行
```
